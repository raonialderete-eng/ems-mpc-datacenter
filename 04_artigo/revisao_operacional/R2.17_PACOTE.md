# Pacote reproduzível R2.17 — GitHub + Zenodo

**Não publicar enquanto este gate for falso.**

## Gate (HIL congelado)

Publicar só quando **uma** das duas for verdadeira:

1. Existe sessão física em `05_resultados/revisao_operacional/laboratorio/<sessao>/` com `backend=serial` (heurístico e MPC, quatro cenários), metadata de COM/firmware/Quartus, e critérios de aceite do `HANDOFF_DE2115.md`; **ou**
2. O autor decide por escrito submeter só com SIL e declarar HIL pendente no README (não rotular SIL como placa).

**Decisão do autor, 18/09/2026 (opção 2, com HIL parcial):** bancada encerrada neste recorte. Submissão com SIL completo + UART HIL **heurístico** nos quatro cenários curtos (`02_hil_resultados.csv`, `backend=serial`, `controle=heur`) + ADMM no Nios demonstrado, **sem** campanha serial MPC de 1200 passos e **sem** FIL. README público deve repetir esses limites. Gate de *publicação* passa a **verdadeiro sob a opção 2**; o repositório GitHub/Zenodo ainda não foi aberto (próximo passo, privado até a submissão).

## Incluir na release

| Conteúdo | Caminho atual |
|---|---|
| Código auditado MATLAB | `01_matlab_base/revisao_operacional/` |
| Jobs e sementes | `04_artigo/revisao_operacional/jobs.json`, `criar_jobs.py` |
| Dataset derivado + hashes | `05_resultados/revisao_operacional/dataset_v1/` (`manifest.json`, seed 20260909) |
| Script de perfis | `04_artigo/revisao_operacional/preparar_dataset.py` |
| Consolidação 11/09 | `05_resultados/revisao_operacional/consolidacao_corrigida_20260911_v1/` (CSVs + `status.json`, não copiar `integridade_inicio_*`) |
| SIL FPGA-ready | `05_resultados/hil_de2115/` + `laboratorio/sil_preparacao_01/` |
| HIL físico | pasta `laboratorio/<sessao serial>/` quando existir |
| Firmware/Qsys fontes | `07_fpga/` (`.tcl`, C golden, protocolo); bitstream: hash do `.sof` se a licença Intel impedir upload |
| Formulação | `04_artigo/revisao_operacional/preparacao_artigo_20260911_v1/` |
| LICENSE + README de reprodução | a criar na raiz do repo público |

Dataset bruto ~1 GB: **não** subir por padrão. README aponta a fonte oficial + hashes do `manifest.json`. Conferir termos DIPLOEE/NVML antes.

## Excluir

`.venv_revisao`, `output_files` Quartus, cópias `integridade_inicio_*` / `auditoria_integridade_01`, carta interna, credenciais, MATs dos 1.755 jobs se o arquivo for enorme (CSVs + script; MATs opcionais em Zenodo separado).

## Procedimento quando o gate passar

1. Repo GitHub (privado até a submissão).
2. README: MATLAB R2025b + Optimization Toolbox; o que não reproduz sem DE2-115/Quartus 20.1; SIL ≠ instalação MW.
3. Tag `v1.0.0-ieee-access-rev`.
4. Zenodo a partir da release → DOI.
5. Citar DOI na carta (R2.17) e no artigo.

Este ficheiro não é o depósito. É a lista de empacotamento.
