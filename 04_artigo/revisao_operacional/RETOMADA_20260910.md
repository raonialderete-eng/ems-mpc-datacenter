# Retomada de 10/09/2026

O processo anterior foi encontrado encerrado, sem MATLAB ativo. Havia 153 resultados completos: principal 35/35, fairness 94/105 e ablação 24/28. O último caso iniciado foi j00154, sem resultado completo salvo. A causa do encerramento não foi determinada.

O lock antigo foi preservado como `05_resultados/revisao_operacional/campanha_v1/INTERRUPTED_20260910_065147.lock`. Nenhum resultado completo foi removido. O mecanismo original de fingerprint continua obrigatório; uma incompatibilidade impede a retomada.

A primeira tentativa de retomada falhou na verificação de integridade porque o Python fornecido pelo aplicativo estava sem SciPy. O erro e o diretório parcial de integridade foram preservados. Foi criado `.venv_revisao` com NumPy, SciPy e Matplotlib, exclusivo para as ferramentas operacionais. O dataset não foi baixado novamente. O código MATLAB e os parâmetros da campanha não foram alterados.

Para retomadas futuras, usar `./04_artigo/revisao_operacional/retomar_ambiente_local.ps1`. Não executar duas instâncias. Em caso de lock, conferir os processos e logs antes de qualquer intervenção. As versões Python instaladas estão em `requirements_retomada_20260910.txt`; recriar o ambiente com Python 3.12 e `python -m pip install -r` apontando para esse arquivo, se necessário.

Os logs `retomada_*.stdout.log`, `retomada_*.stderr.log` e `pipeline_*.jsonl` estão em `05_resultados/revisao_operacional/`. O pipeline consolida os resultados e verifica novamente a integridade após as simulações. A conclusão automática da execução não substitui a revisão científica.

Os handoffs anteriores permanecem disponíveis. Fairness, ablações e demais grupos só devem ser considerados completos após conferir os contadores; a análise probabilística e a atualização final do handoff ainda dependem do término da campanha. Compilação C, validação física e redação do artigo continuam com os limites registrados no plano.
